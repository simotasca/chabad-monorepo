{
  "license": "UNLICENSED",
  "scripts": {
    "dev": "docker compose up -d && yarn logs",
    "logs": "docker compose logs website -f",
    "stop": "docker compose down",
    "types": "cd apps/website; npm run db:types"
  }
}